deploy(){
  trigger hello
  trigger backup
  trigger extract 
  trigger build
  trigger runtests
  trigger start
}

checkout(){
  alias git="git --work-tree=$(pwd) --git-dir=$(pwd)/.git"
  git checkout -f
}
# wo  9 feb 2022 13:02:23 CET
